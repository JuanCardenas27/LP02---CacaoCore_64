#define size 9
#include "float_function.choco" {insertion_sort}
let n : int = size
let a: float[size] = 99.1,1.2,8.3,4.5,2.2,10.4,2.8,10.9,11.3



insertion_sort()

let elem: float

for (let k: int = 0, k < n, set k += 1) {
    set elem = a[k]
    show elem
}

